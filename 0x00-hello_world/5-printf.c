#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	printf("%s\n", "with proper grammer, but the outcome is a piece of art,");
       return (0);
}       
